mixin button(text, className, href, icon)
	if href
		a.button(class=className, href=href)&attributes(attributes)
			if icon
				+icon(icon)
			if text
				span!= text
	else
		button.button(class=className)&attributes(attributes)
			if icon
				+icon(icon)
			if text
				span!= text
